apiVersion: v1
kind: Service
metadata:
  name: {{ include "car-api.fullname" . }}-mkdocs
  labels:
    {{- include "car-api.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "car-api-mkdocs.selectorLabels" . | nindent 4 }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80