apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "car-api.fullname" . }}-test-connection"
  labels:
    {{- include "car-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "car-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
